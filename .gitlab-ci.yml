default:
  tags:
    - devops-project

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - sudo docker-compose build

test:
  stage: test
  script:
    - echo "Running Tests ........."

deploy:
  stage: deploy
  script:
    - sudo docker-compose up -d