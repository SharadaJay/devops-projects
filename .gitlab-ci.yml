default:
  tags:
    - devops-project

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - pwd && docker-compose build

test:
  stage: test
  script:
    - pwd && echo "Running Tests !!!!!!"

deploy:
  stage: deploy
  script:
    - pwd && docker-compose up -d
